{
  "name": "medicore-hms",
  "private": true,
  "scripts": {
    "typecheck": "npm --prefix backend run typecheck && npm --prefix frontend run typecheck",
    "test": "npm --prefix backend run test:ci && npm --prefix frontend run test:run",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "lint": "npm --prefix backend run lint && npm --prefix frontend run lint",
    "verify": "npm run typecheck && npm run test && npm run build && npm run lint",
    "verify:source": "node scripts/verify-source.mjs"
  }
}
